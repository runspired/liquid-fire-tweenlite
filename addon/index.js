import { isAnimating, finish, timeSpent, animate, stop } from './lib/animate';
import TransitionPromise from './lib/transition-promise';

export {
  animate,
  stop,
  isAnimating,
  finish,
  timeSpent,
  TransitionPromise
};
